# 02. 스펙 파일 작성 및 검증

## 스펙 종류

루트 프로젝트는 네 종류의 스펙을 사용합니다.

- 학습: `PipelineSpec`
- 서빙: `ServingSpec`
- 튜닝: `TuneSpec`
- 벤치마크: `BenchmarkSpec`

각 예제 파일은 `configs/` 아래에 있습니다.

## 검증 명령

```bash
kfp-workflow spec validate --spec configs/pipelines/mambasl_cmapss_smoke.yaml
kfp-workflow spec validate --spec configs/serving/mambasl_cmapss_serve.yaml --type serving
kfp-workflow spec validate --spec configs/tuning/mambasl_cmapss_tune.yaml --type tune
kfp-workflow spec validate --spec configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml --type benchmark
```

## 무엇을 먼저 채워야 하나

### 공통

- `metadata.name`
- `runtime.*`
- `storage.*`

### 학습/튜닝

- `model.name`
- `dataset.*`
- `train.*`

### 서빙

- `metadata.name`
- `namespace`
- `model_pvc`
- `model_subpath`
- `runtime`
- `model_name`

### 벤치마크

- 임시 서빙용 `model`
- 입력 재생을 위한 `scenario`
- 수집할 `metrics`

벤치마크 명령은 현재 YAML 예제 스펙뿐 아니라 Python benchmark definition 파일도 받을 수 있습니다.

## `--set`으로 검증 전 수정

```bash
kfp-workflow spec validate \
  --spec configs/pipelines/mambasl_cmapss_smoke.yaml \
  --set train.max_epochs=10 \
  --set model.config.d_model=128
```

배열 인덱스도 지원합니다.

```bash
kfp-workflow spec validate \
  --spec configs/pipelines/mambasl_cmapss_smoke.yaml \
  --set dataset.config.fd[0].fd_name=FD003
```

## JSON 출력

```bash
kfp-workflow --json spec validate \
  --spec configs/tuning/mambasl_cmapss_tune.yaml \
  --type tune
```

자동화 스크립트에서 검증 결과를 파싱할 때 유용합니다.

## 다음 단계

[03_파이프라인_컴파일_및_제출.md](/home/scouter/proj_2026_1_etri/test/examples/03_%ED%8C%8C%EC%9D%B4%ED%94%84%EB%9D%BC%EC%9D%B8_%EC%BB%B4%ED%8C%8C%EC%9D%BC_%EB%B0%8F_%EC%A0%9C%EC%B6%9C.md)
