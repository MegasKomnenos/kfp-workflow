# 12. 벤치마크 워크플로우

## 개요

kfp-workflow의 벤치마크는 다음 세 요소를 하나의 워크플로우로 묶습니다:

- **model** — 임시로 배포할 KServe `InferenceService`
- **scenario** — 어떤 데이터를 어떤 순서와 주기로 보낼지 정의
- **metrics** — 시나리오 실행 중 어떤 메트릭을 수집할지 정의

이 프로젝트에 포함된 벤치마크는 두 가지입니다:

- **`mambasl_cmapss_kepler_smoke.yaml`** — MambaSL 모델을 임시 배포하고 C-MAPSS FD001 일부를 1Hz로 재생하면서 Kepler로 predictor container energy를 수집합니다.
- **`mambasl_cmapss_test.yaml`** — MambaSL 모델을 임시 배포하고 C-MAPSS FD001 전체 test set에 대한 예측을 수행한 뒤 F1/precision/recall/accuracy를 계산합니다.

## 아키텍처

```
BenchmarkSpec
├── model
│   └── KServe InferenceService 배포 설정
├── scenario
│   ├── dataset  → 입력 레코드/섹션 생성
│   └── pipeline → 요청 순서, 속도, 실패 처리
└── metrics
    └── 시나리오 시작/종료 전후 메트릭 수집
```

현재 기본 제공 구현:

**데이터셋 소스:**
- `cmapss-timeseries` — C-MAPSS test set에서 슬라이딩 윈도우 섹션을 모두 생성 (스트리밍/지연 벤치마크용)
- `cmapss-test-set` — C-MAPSS test set에서 **유닛당 마지막 윈도우 하나만** 생성, `make_last_windows` 방식 (정확도 평가용)

**파이프라인:**
- `sequential-replay` — 섹션을 설정된 속도(Hz)로 predictor에 순차 전송
- `test-eval` — 속도 제한 없이 섹션을 한 번씩 전송 (test set 정확도 평가에 최적)

**메트릭:**
- `kepler-energy` — `kepler_container_joules_total` delta 계산 (Prometheus/Kepler)
- `cmapss-test` — `RUL_FDxxx.txt` 정답과 예측값을 비교해 이진 분류 지표 반환: `f1_score`, `precision`, `recall`, `accuracy`

## shipped smoke benchmark

### 최상위 BenchmarkSpec

```yaml
# configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml
metadata:
  name: mambasl-cmapss-benchmark-smoke
  description: MambaSL C-MAPSS benchmark smoke test with Kepler energy capture
  version: v1

runtime:
  namespace: kubeflow-user-example-com
  image: kfp-workflow:latest
  image_pull_policy: IfNotPresent
  service_account: default-editor
  host: "http://127.0.0.1:8888"
  port_forward_namespace: kubeflow
  port_forward_service: svc/ml-pipeline

storage:
  data_pvc: dataset-store
  model_pvc: model-store
  results_pvc: benchmark-store
  storage_class: local-path
  data_size: 8Gi
  model_size: 8Gi
  results_size: 8Gi
  data_mount_path: /mnt/data
  model_mount_path: /mnt/models
  results_mount_path: /mnt/benchmarks

model:
  model_name: mambasl-cmapss
  model_version: v1
  model_pvc: model-store
  model_subpath: mambasl-cmapss/v1
  predictor_image: kfp-workflow:latest
  service_name: mbsl-bench-smoke
  cleanup: true
  wait_timeout: 300

scenario:
  ref: scenarios/mambasl_cmapss_timeseries_smoke.yaml

metrics:
  - ref: metrics/kepler_predictor_energy.yaml
```

핵심 포인트:

- `storage.results_pvc`가 벤치마크 결과 저장소입니다.
- `model` 블록은 벤치마크 동안만 사용할 임시 `InferenceService`를 정의합니다.
- `scenario`와 `metrics`는 별도 YAML로 분리해 재사용할 수 있습니다.

### scenario ref

```yaml
# configs/benchmarks/scenarios/mambasl_cmapss_timeseries_smoke.yaml
kind: inline
dataset:
  kind: cmapss-timeseries
  config:
    fd:
      - fd_name: FD001
        unit_ids: [1, 2, 3]
        max_sections: 5
    feature_mode: settings_plus_sensors
    norm_mode: condition_minmax
pipeline:
  kind: sequential-replay
  config:
    interval_hz: 1.0
    request_timeout: 30
```

이 시나리오는 FD001에서 일부 유닛만 읽고, 최대 5개 섹션만 1초 간격으로 전송하는 작은 smoke benchmark입니다.

### metric ref

```yaml
# configs/benchmarks/metrics/kepler_predictor_energy.yaml
kind: kepler-energy
config:
  prometheus_url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
  metric_name: kepler_container_joules_total
  container_name: kserve-container
  mode: dynamic
  settle_seconds: 20
  poll_interval_seconds: 1
```

`settle_seconds: 20`은 이 로컬 클러스터에서 Kepler counter 반영이 지연되는 특성을 반영한 값입니다. 더 짧게 줄이면 마지막 요청이 energy delta에 반영되지 않을 수 있습니다.

## test-set 정확도 벤치마크

### 배경: C-MAPSS test set 구조

C-MAPSS는 FD별로 세 파일을 독립적으로 제공합니다:

- `train_FD001.txt` — 완전한 run-to-failure 시계열 (학습 유닛)
- `test_FD001.txt` — 고장 전 임의 시점에서 잘린 시계열 (학습과 **완전히 다른 유닛**)
- `RUL_FD001.txt` — 각 test 유닛의 마지막 관측 시점 기준 실제 잔여 수명 (1유닛당 1값)

정확도 평가의 표준 방법은 각 test 유닛의 **마지막 윈도우**를 예측하고 `RUL_FD001.txt` 정답과 비교하는 것입니다. `cmapss-test-set` 소스와 `test-eval` 파이프라인은 이 흐름에 맞게 설계되어 있습니다.

### 최상위 BenchmarkSpec

```yaml
# configs/benchmarks/mambasl_cmapss_test.yaml
metadata:
  name: mambasl-cmapss-benchmark-test
  description: MambaSL C-MAPSS FD001 test-set accuracy benchmark (F1 / precision / recall / accuracy)
  version: v1

model:
  model_name: mambasl-cmapss
  model_version: v1
  model_pvc: model-store
  model_subpath: mambasl-cmapss/v1
  predictor_image: kfp-workflow:latest
  service_name: mbsl-bench-test
  cleanup: true
  wait_timeout: 300
  # ... (storage, runtime은 kepler smoke와 동일)

scenario:
  ref: scenarios/mambasl_cmapss_test_fd001.yaml

metrics:
  - ref: metrics/cmapss_test_fd001.yaml
```

### scenario ref

```yaml
# configs/benchmarks/scenarios/mambasl_cmapss_test_fd001.yaml
kind: inline
dataset:
  kind: cmapss-test-set          # 유닛당 마지막 윈도우 1개만 생성
  config:
    fd:
      - fd_name: FD001           # FD001 전체 test 유닛 (unit_ids 미지정 = 전체)
    feature_mode: settings_plus_sensors
    norm_mode: condition_minmax
pipeline:
  kind: test-eval                # 속도 제한 없이 한 번씩 전송
  config:
    request_timeout: 30
```

`cmapss-timeseries` + `sequential-replay` 조합과의 차이점:

| | `cmapss-timeseries` + `sequential-replay` | `cmapss-test-set` + `test-eval` |
|---|---|---|
| 전송 섹션 수 | 유닛당 전체 윈도우 수 | 유닛당 1개 (마지막 윈도우) |
| 속도 제한 | `interval_hz` | 없음 |
| 용도 | 에너지/지연 측정 | 예측 정확도 측정 |

### metric ref

```yaml
# configs/benchmarks/metrics/cmapss_test_fd001.yaml
kind: cmapss-test
config:
  rul_threshold: 30    # RUL ≤ 30 → 양성(near failure), RUL > 30 → 음성(healthy)
  average: binary      # sklearn F1/precision/recall 평균 방식
```

`rul_threshold`는 예측값과 정답 모두에 동일하게 적용됩니다. 기본값 30은 C-MAPSS 문헌에서 널리 사용되는 임계값입니다.

### 결과 payload 예시

```json
{
  "benchmark_name": "mambasl-cmapss-benchmark-test",
  "status": "succeeded",
  "scenario": {
    "request_count": 100
  },
  "metrics": {
    "metric_0": {
      "f1_score": 0.82,
      "precision": 0.85,
      "recall": 0.79,
      "accuracy": 0.88,
      "n_evaluated": 100,
      "n_positive": 43,
      "n_predicted_positive": 41,
      "rul_threshold": 30.0,
      "average": "binary"
    }
  }
}
```

### 실행

```bash
# 스펙 검증
kfp-workflow spec validate \
    --spec configs/benchmarks/mambasl_cmapss_test.yaml \
    --type benchmark

# 컴파일
kfp-workflow benchmark compile \
    --spec configs/benchmarks/mambasl_cmapss_test.yaml \
    --output pipelines/mambasl_cmapss_test.yaml

# 제출
kfp-workflow benchmark submit \
    --spec configs/benchmarks/mambasl_cmapss_test.yaml

kfp-workflow pipeline run wait <run_id>
kfp-workflow benchmark download <run_id>
```

기대 결과:
- `status == "succeeded"`
- `scenario.request_count == 100` (FD001 test 유닛 수)
- `metrics.metric_0.f1_score` — RUL 30 임계값 기준 이진 F1

## YAML과 Python 확장

scenario와 metrics는 YAML ref뿐 아니라 Python 심볼로도 정의할 수 있습니다.

```yaml
scenario:
  ref: benchmark_defs/my_scenario.py:SCENARIO

metrics:
  - ref: benchmark_defs/my_metric.py:METRIC
```

또는 dataset / pipeline / metric 노드에서 `kind: python`과 `ref:`를 직접 둘 수 있습니다. `spec validate --type benchmark`는 ref를 실제로 해석하고 표준 인터페이스를 검사합니다.

## 실행 순서

### 1. 스펙 검증

```bash
kfp-workflow spec validate \
    --spec configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml \
    --type benchmark
```

성공하면 materialized benchmark payload가 출력됩니다. 여기서 `scenario`와 `metrics` ref가 실제로 해석되었는지 먼저 확인하는 것이 좋습니다.

### 2. benchmark 전용 PVC 부트스트랩

```bash
kfp-workflow cluster bootstrap \
    --spec configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml \
    --type benchmark
```

이 명령은 최소 세 PVC를 준비합니다:

- `dataset-store`
- `model-store`
- `benchmark-store`

미리보기만 하려면:

```bash
kfp-workflow cluster bootstrap \
    --spec configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml \
    --type benchmark \
    --dry-run
```

### 3. 컴파일

```bash
kfp-workflow benchmark compile \
    --spec configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml \
    --output pipelines/mambasl_cmapss_kepler_smoke.yaml
```

학습 파이프라인과 달리 benchmark DAG는 side effect가 크므로, 이 프로젝트의 benchmark task들은 KFP cache를 사용하지 않도록 컴파일됩니다.

### 4. 제출

```bash
kfp-workflow benchmark submit \
    --spec configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml
```

필요하면 `--namespace`, `--host`, `--user`, `--existing-token`, `--cookies`, `--set`을 그대로 사용할 수 있습니다.

예시:

```bash
kfp-workflow benchmark submit \
    --spec configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml \
    --set model.service_name=mbsl-bench-smoke-alt
```

### 5. 실행 대기 및 상태 확인

```bash
kfp-workflow pipeline run wait <run_id>
kfp-workflow pipeline run get <run_id>
kfp-workflow benchmark get <run_id>
kfp-workflow benchmark download <run_id>
```

벤치마크 실행도 KFP run이므로 모니터링은 기존 `pipeline run *` 명령을 그대로 사용합니다.

## 결과 확인

### 결과 파일 위치

benchmark runtime은 다음 경로 패턴으로 결과를 저장합니다:

```text
/mnt/benchmarks/benchmark-results/<benchmark-name>/<timestamp>-<benchmark-pod-hostname>/results.json
```

정확한 최종 경로는 `results.json` 안의 `results_path` 필드를 기준으로 확인하는 것이 가장 안전합니다.

실제 payload에는 최소 다음 필드가 들어 있습니다:

```json
{
  "benchmark_name": "mambasl-cmapss-benchmark-smoke",
  "status": "succeeded",
  "target": { "...": "..." },
  "scenario": {
    "request_count": 5
  },
  "metrics": {
    "metric_0": {
      "delta_joules": 27.963000000000008
    }
  }
}
```

### PVC에서 결과 읽기

기본 확인 경로는 `kfp-workflow benchmark get <run_id>` 와 `kfp-workflow benchmark download <run_id>` 입니다. 아래 방법은 CLI 결과와 PVC 상태를 직접 대조해야 할 때만 쓰는 troubleshooting 절차입니다.

```bash
kubectl apply -f - <<'YAML'
apiVersion: v1
kind: Pod
metadata:
  name: benchmark-results-viewer
  namespace: kubeflow-user-example-com
spec:
  restartPolicy: Never
  containers:
  - name: shell
    image: busybox:1.36
    command: ["sh", "-c", "sleep 3600"]
    volumeMounts:
    - name: bench
      mountPath: /mnt/benchmarks
  volumes:
  - name: bench
    persistentVolumeClaim:
      claimName: benchmark-store
YAML
kubectl wait --for=condition=Ready pod/benchmark-results-viewer -n kubeflow-user-example-com --timeout=120s
kubectl exec -n kubeflow-user-example-com benchmark-results-viewer -- \
  sh -lc 'find /mnt/benchmarks -name results.json | sort'
kubectl delete pod -n kubeflow-user-example-com benchmark-results-viewer --ignore-not-found=true
```

특정 run 디렉터리를 알면 `cat`으로 내용을 바로 확인할 수 있습니다.

## 검증된 smoke benchmark 결과

이 프로젝트에서 2026년 3월 29일에 로컬 클러스터로 검증한 실행 결과는 다음과 같습니다:

- Run ID: `ba893c5b-14bb-4fde-8229-a040127ee36e`
- Workflow: `mambasl-cmapss-benchmark-smoke-2nx2f`
- Finished at: `2026-03-29T10:31:17Z`
- `scenario.request_count == 5`
- `metrics.metric_0.delta_joules == 27.963000000000008`

실무적으로는 exact delta 값보다 아래 조건이 더 중요합니다:

- workflow status가 `Succeeded`
- predictor `InferenceService`가 준비되었다가 실행 후 정리됨
- `scenario.request_count == 5`
- `metrics.metric_0.delta_joules > 0`

## 문제 해결

### benchmark-store PVC가 없음

학습용 bootstrap이 아니라 benchmark 타입으로 실행해야 합니다:

```bash
kfp-workflow cluster bootstrap \
    --spec configs/benchmarks/mambasl_cmapss_kepler_smoke.yaml \
    --type benchmark
```

### 마지막 energy delta가 0에 가까움

이 로컬 클러스터에서는 Kepler metric 반영이 지연됩니다. `configs/benchmarks/metrics/kepler_predictor_energy.yaml`의 `settle_seconds`를 너무 짧게 줄이지 마세요.

### predictor 이미지 pull 실패

학습 파이프라인과 동일하게, 로컬 클러스터에서는 helper pod + `nerdctl` import 흐름을 사용합니다. 절차는 [09. Docker 이미지 빌드](09_Docker_이미지_빌드.md)를 따르세요.

## 다음 단계

벤치마크 결과를 비교 가능한 실험으로 늘리려면 [05. CLI 설정 오버라이드](05_CLI_설정_오버라이드.md)로 돌아가 `--set` 기반 변형 실행을 조합하세요.
